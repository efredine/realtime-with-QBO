type Query {
  currentUser: User
  getAuthUri: AuthUri
}

type AuthUri {
  uri: String!
}
type SuccessMessage {
  message: String
}

type User {
  id: ID!
  name: String!
}

type Mutation {
  connect: User
  signOut: SuccessMessage
}
